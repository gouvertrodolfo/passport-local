<!DOCTYPE html>
<html lang="es">

<head>
    <%- include('../partials/head') %>
</head>

<body>

    <% if(user!==undefined){ %>
        <div class="container p-3 my-3 bg-light text-black">

            <%- include('../partials/navbar_logout', {user:user}) %>
                <% } else { %>
                    <%- include('../partials/navbar_login') %>
                        <% } %>
        </div>


        <div class="container p-3 my-3 bg-light text-black">
            <h1>
                <%= titulo %>
            </h1>

            <% if(user!==undefined){ %>
                <%- include('../partials/Formulario') %>
                    <% } %>

                        <div id=grillaProductos> </div>
        </div>


        <% if(user!==undefined){ %>
            <div class="container p-3 my-3 bg-light text-black">
                <%- include('../partials/chat',{user:user}) %>
            </div>
            <% } %>

</body>

</html>